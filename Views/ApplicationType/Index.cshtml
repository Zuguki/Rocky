@model IEnumerable<Rocky.Models.ApplicationType>

@{
    ViewBag.Title = "ApplicationType";
    Layout = "_Layout";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h1 class="text-primary">Application Types</h1>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="ApplicationType" asp-action="Create" class="btn btn-primary">
                <i class="far fa-plus-square"></i> &nbsp; Create new application
            </a>
        </div>
    </div>
    
    <br /><br />
    
    @if (Model.Any())
    {
        <table class="table table-bordered table-striped" style="width: 100%">
            <thead>
            <tr>
                <th>
                    Application Name
                </th>
            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td style="width: 700%">@item.Name</td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="ApplicationType" asp-route-id="@item.Id" asp-action="Edit" class="btn btn-primary mx-2">
                                <i class="fas fa-edit"></i>
                            </a>
                            <a asp-controller="ApplicationType" asp-route-id="@item.Id" asp-action="Delete" class="btn btn-danger mx-2">
                                <i class="far fa-trash-alt"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    }
    else
    {
        <p>No apps exists.</p>
    }
    
</div>
